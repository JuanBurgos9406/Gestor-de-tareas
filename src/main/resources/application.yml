spring:
  application:
    name: todotask

  datasource:
    url: jdbc:postgresql://localhost:5432/tareas_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver

    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
